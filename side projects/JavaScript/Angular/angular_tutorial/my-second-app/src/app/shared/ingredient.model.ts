export class Ingredient{
  //automatically created properties by typescript instead of writing them
  //they are written behind the scenes
  constructor(public name: string, public amount: number){}
}